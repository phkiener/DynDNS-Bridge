<Router AppAssembly="@MainLayout.Assembly" NotFoundPage="@typeof(NotFoundPage)">
    <Found>
        <RouteView RouteData="context" DefaultLayout="@MainLayout" />
    </Found>
</Router>

@code {

    private static readonly Type MainLayout = typeof(Layout);

}
